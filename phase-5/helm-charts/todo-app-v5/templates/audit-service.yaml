apiVersion: v1
kind: Service
metadata:
  name: {{ include "todo-app-v5.fullname" . }}-audit-service
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "todo-app-v5.labels" . | nindent 4 }}
    app: audit-service
spec:
  selector:
    app: audit-service
  ports:
    - protocol: TCP
      port: {{ .Values.auditService.service.port }}
      targetPort: {{ .Values.auditService.service.port }}
  type: {{ .Values.auditService.service.type }}