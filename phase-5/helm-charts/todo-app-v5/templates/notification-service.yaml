apiVersion: v1
kind: Service
metadata:
  name: {{ include "todo-app-v5.fullname" . }}-notification-service
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "todo-app-v5.labels" . | nindent 4 }}
    app: notification-service
spec:
  selector:
    app: notification-service
  ports:
    - protocol: TCP
      port: {{ .Values.notificationService.service.port }}
      targetPort: {{ .Values.notificationService.service.port }}
  type: {{ .Values.notificationService.service.type }}