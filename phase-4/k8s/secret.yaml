# Task ID: T310
# Kubernetes Secret manifest (template only)
# Note: Values should be base64 encoded
# To encode: echo -n 'your_value' | base64
apiVersion: v1
kind: Secret
metadata:
  name: todo-secrets
  namespace: todo-app
type: Opaque
data:
  # Base64 encoded values
  # DATABASE_URL: <base64-encoded-database-url>
  # OPENAI_API_KEY: <base64-encoded-openai-api-key>
  # BETTER_AUTH_SECRET: <base64-encoded-auth-secret>